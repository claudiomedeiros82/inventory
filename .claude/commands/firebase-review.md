This command has been replaced by `/backend-review`, which supports Firebase, Supabase, Appwrite, PocketBase, Railway, and custom REST backends.

Run `/backend-review` instead — it auto-detects your backend and runs the right checks.
